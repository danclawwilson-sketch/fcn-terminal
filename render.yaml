services:
  - type: web
    name: fcn-terminal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python fcn-terminal-app.py
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.12.0
